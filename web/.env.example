# Phase 6 Next.js app — talks only to the Phase 5 Query API.
# Copy to web/.env.local (or set in the repo root .env and export before `npm run dev`).

API_BASE_URL=http://127.0.0.1:8000
API_SHARED_SECRET=
