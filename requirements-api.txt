# Query API only. No torch / sentence-transformers / sklearn.
# Keep in sync with repo-root requirements.txt (Vercel FastAPI install).
# The API Docker image installs this, then
# `pip install --no-deps -e .` so pyproject's ML extras are skipped.
psycopg[binary]>=3.2
pydantic>=2.7
pydantic-settings>=2.4
python-dotenv>=1.0
openai>=1.40
numpy>=1.26
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
setuptools>=68
